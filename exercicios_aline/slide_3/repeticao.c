#include <stdio.h>

int main()
{
    int termos;
    printf("Digite o numero de termos da sequencia: ");
    scanf("%i", &termos);

    float vetor[termos];
    int termos_diferentes = 0;
    printf("Digite agora os termos da sequencia: \n");
    for (int i = 0; i < termos; i++) {
        scanf("%f", &vetor[i]);
    }

    int termos_diferentes = 0;
    for i
}