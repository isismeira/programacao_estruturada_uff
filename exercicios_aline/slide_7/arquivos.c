#include <stdio.h>

/*
    FILE *fp;
    fp = fopen("entrada.txt", "r"); //ler um arquivo
    fp = fopen("saida.txt", "w"); //escrever um arquivo

    if (fp == NULL) { // lidando com um erro na abertura do arquivo
        printf("Erro na abertura do arquivo.\n");
        exit(1)
    }

    fclose(fp) // é necessário fechar o arquivo

    int a;
    float b;
    fscanf(fp, "%d %f", &a, &b) // lendo elemento do arquivo de entrada

    fprintf(fp, "Valores: %d %f \n", a , b) // escrevendo o arquivo de saida

*/